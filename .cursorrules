Primary project rules live in `AGENTS.md`.
Search workflow lives in `AI_WORKFLOW.md`.
Start with `AGENTS.md`, then `AI_WORKFLOW.md`, then `PROJECT_MAP.md`, then the nearest directory `README.md`.
Before changing auth, session, or admin behavior, read `backend/tests/test_api.py`.
