// Thin route entry that renders the shared research-group workspace client component.
import { TeamWorkspace } from "../../components/team-workspace";

export default function ResearchGroupsPage() {
  return <TeamWorkspace />;
}
